/**
 * Customer display: no POS nav or top bar. Full-screen read-only display only.
 */
export default function CustomerDisplayLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
